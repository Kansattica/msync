language: cpp
env:
  - BUILDTYPE=Release
  #- BUILDTYPE=MinSizeRel
os: linux
dist: bionic
compiler:
  - clang
  - gcc
matrix:
  include:
  - os: windows
    compiler: msvc
    env: BUILDTYPE=Release 
  #- os: windows
    #compiler: msvc
    #env: BUILDTYPE=MinSizeRel 
  
before_install:
  - if [ "$TRAVIS_COMPILER" = "gcc" ]; then sudo -E apt-get -yq --no-install-suggests --no-install-recommends install g++-8 && CC=gcc-8 && CXX=g++-8; fi
  - if [ "$TRAVIS_COMPILER" = "clang" ]; then sudo dpkg -l libc++-dev; sudo -E apt-get -yq --no-install-suggests --no-install-recommends install libc++-dev; fi

script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_BUILD_TYPE=$BUILDTYPE -DUSE_SYSTEM_CURL=OFF ..
  - cmake --build . --config $BUILDTYPE --parallel 2
  - if [ "$TRAVIS_OS_NAME" = "windows" ]; then EXELOCATION="./$BUILDTYPE/msync.exe"; else EXELOCATION="./msync"; fi

after_success:
  - echo "Binary size:"
  - du -h $EXELOCATION
  - $EXELOCATION
  - $EXELOCATION -n 420 -n 22 -n 69 -v